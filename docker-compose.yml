version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finzuu-company-service
    restart: always
    command: uvicorn app.main:app --host 0.0.0.0 --port 12041 --reload
    ports:
      - '12041:12041'
    volumes:
      - ./:/finzuu
